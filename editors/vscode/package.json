{
  "name": "sivtr-vscode",
  "displayName": "sivtr",
  "description": "Launch the sivtr AI session picker from VS Code.",
  "version": "0.1.3",
  "publisher": "ariestar",
  "icon": "icon.png",
  "license": "Apache-2.0",
  "repository": {
    "type": "git",
    "url": "https://github.com/Ariestar/sivtr.git",
    "directory": "editors/vscode"
  },
  "engines": {
    "vscode": "^1.90.0"
  },
  "extensionKind": [
    "workspace"
  ],
  "categories": [
    "Other"
  ],
  "main": "./out/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "sivtr.pickAgent",
        "title": "Sivtr: Pick AI Session"
      }
    ],
    "keybindings": [
      {
        "command": "sivtr.pickAgent",
        "key": "alt+y",
        "mac": "cmd+alt+y",
        "when": "!terminalFocus"
      }
    ],
    "configuration": {
      "title": "sivtr",
      "properties": {
        "sivtr.command": {
          "type": "string",
          "default": "sivtr",
          "description": "Command used to launch sivtr."
        },
        "sivtr.args": {
          "type": "array",
          "default": [
            "hotkey-pick-agent",
            "--cwd",
            ".",
            "--provider",
            "all"
          ],
          "items": {
            "type": "string"
          },
          "description": "Arguments passed to the sivtr command."
        },
        "sivtr.reuseTerminal": {
          "type": "boolean",
          "default": true,
          "description": "Reuse the existing sivtr terminal when launching the picker."
        },
        "sivtr.closeTerminalOnSuccess": {
          "type": "boolean",
          "default": true,
          "description": "Close the sivtr terminal when the picker exits successfully."
        },
        "sivtr.terminalName": {
          "type": "string",
          "default": "sivtr",
          "description": "Name of the VS Code terminal used by sivtr."
        }
      }
    }
  },
  "scripts": {
    "compile": "tsc -p ./",
    "test": "bun run compile && node --test out/commandLine.test.js",
    "watch": "tsc -watch -p ./",
    "package": "bun run compile && node node_modules/@vscode/vsce/vsce package --no-dependencies --no-yarn --allow-missing-repository"
  },
  "devDependencies": {
    "@types/node": "26.3.0",
    "@types/vscode": "1.134.0",
    "@vscode/vsce": "3.9.2",
    "typescript": "7.0.2"
  }
}
